Вот **README.md** полностью в корректном и чистом **Markdown**
(никакого HTML, только стандартный markdown-синтаксис).

Скопируйте как есть:

---

```markdown
# Go-Neuro — простая нейросеть на Go

Go-Neuro — это учебная реализация многослойной нейронной сети (MLP) на языке Go **с нуля**, без зависимостей.  
Проект демонстрирует, как устроены слои, активации, обратное распространение ошибки (backpropagation)  
и различные методы обучения.

---

## 📁 Структура проекта



project/
│   main.go
│   README.md
│
└── nn/
├── activations.go
├── layer.go
├── network.go
│
├── trainers/
│     ├── sgd.go
│     └── adam.go
│
└── dataset/
├── dataset.go
├── xor.go
├── and.go
└── or.go



---

## 🚀 Запуск проекта

```bash
go run ./...
````

---

## 🛠 Пример использования

```go
ds := dataset.MustGet("xor")

net := nn.NewNetwork([]int{2, 2, 1})
trainer := &trainers.SGDTrainer{LearningRate: 0.5}

net.Fit(trainer, ds.Samples, ds.Targets, 20000)
```

Пример результата:

```
Epoch 0, loss=0.250000
Epoch 1000, loss=0.015124
Epoch 20000, loss=0.000012

Input: [0 1] → 0.9821 (rounded 1), Target: [1]
```

---

## 📦 Датасеты

Каждый датасет регистрируется при загрузке пакета:

```go
func init() {
    Register("xor", DataSet{
        Samples: [][]float64{
            {0, 0}, {0, 1}, {1, 0}, {1, 1},
        },
        Targets: [][]float64{
            {0}, {1}, {1}, {0},
        },
    })
}
```

Получение датасета:

```go
ds := dataset.MustGet("xor")
```

---

## 🏋️ Методы обучения

Каждый алгоритм реализует интерфейс:

```go
type Trainer interface {
    Update(net *Network, samples, targets [][]float64, epochs int)
}
```

### Реализованные тренеры

| Имя           | Тип               | Описание                                 |
| ------------- | ----------------- | ---------------------------------------- |
| `SGDTrainer`  | Градиентный спуск | Полный backpropagation                   |
| `AdamTrainer` | Adam              | Подготовленный шаблон, легко расширяется |

---

## 🔧 Добавление нового алгоритма обучения

1. Создайте файл в `nn/trainers/`:

```
nn/trainers/mytrainer.go
```

2. Реализуйте интерфейс:

```go
type MyTrainer struct {
    LearningRate float64
}

func (t *MyTrainer) Update(net *nn.Network, samples, targets [][]float64, epochs int) {
    // логика обучения
}
```

3. Используйте:

```go
trainer := &trainers.MyTrainer{LearningRate: 0.01}
net.Fit(trainer, samples, targets, 10000)
```

---

## ✨ Добавление нового датасета

1. Создайте файл:

```
nn/dataset/mydata.go
```

2. Зарегистрируйте датасет:

```go
func init() {
    Register("mydata", DataSet{
        Samples: [][]float64{...},
        Targets: [][]float64{...},
    })
}
```

3. Используйте:

```go
ds := dataset.MustGet("mydata")
```

# Многозадачная нейросеть на Go

Сеть с **2 входами → 6 нейронов скрытого слоя → 3 выхода (XOR, AND, OR)** позволяет обучаться **на всех задачах одновременно**.

## Архитектура сети

```
     Входной слой
    x1       x2
     |        |
     v        v
  -----------------
  |  Скрытый слой |   <- 6 нейронов скрытого слоя
  | h1  h2  h3   |
  | h4  h5  h6   |
  -----------------
     |  |  |  |  |  |
     v  v  v  v  v  v
    /   |   |   |   \
   /    |   |   |    \
  v     v   v   v     v



|       Выходной слой        |
|  y_XOR    y_AND    y_OR  |
----------------------------

```

## Как проходят сигналы

1. **Входной слой → скрытый слой**

Каждый нейрон скрытого слоя вычисляет активацию:

```

a_i = sigmoid(sum_j(w_ij * x_j) + b_i)

```

где:
- `x_j` — входные значения  
- `w_ij` — веса входов для нейрона i  
- `b_i` — смещение  
- `sigmoid` — функция активации  

2. **Скрытый слой → выходной слой**

Каждый выход получает сигнал от всех нейронов скрытого слоя:

```

y_k = sigmoid(sum_i(a_i * w_ki) + b_k)

```

- `y_XOR`, `y_AND`, `y_OR` используют разные комбинации скрытых нейронов.
- Нейроны скрытого слоя, которые мало влияют на задачу, имеют маленькие веса, но **активации считаются всегда**.

3. **Обратное распространение (Backpropagation)**

- Ошибка каждого выхода считается отдельно.
- Градиенты ошибок идут обратно к скрытому слою.
- Веса корректируются так, чтобы сеть училась решать все задачи одновременно.

---

## Пример вычислений для одного входа

```

Вход: x1=1, x2=0

Hidden activations:
h1 = sigmoid(w11*1 + w12*0 + b1)
h2 = sigmoid(w21*1 + w22*0 + b2)
...
h6 = sigmoid(w61*1 + w62*0 + b6)

Outputs:
y_XOR = sigmoid(h1*w_XOR1 + h2*w_XOR2 + h4*w_XOR4 + ... + b_XOR)
y_AND = sigmoid(h3*w_AND3 + h5*w_AND5 + h6*w_AND6 + ... + b_AND)
y_OR  = sigmoid(h1*w_OR1 + h2*w_OR2 + ... + h6*w_OR6 + b_OR)

```

- Каждый выход выбирает оптимальное сочетание скрытых нейронов.  
- Все задачи обучаются **одновременно** на одной сети.

---

## Вывод

- Скрытый слой **всегда активен** для всех входов.  
- Каждая задача использует только те веса к выходу, которые наиболее важны.  
- Многозадачная архитектура позволяет одной сети решать несколько логических задач одновременно.


